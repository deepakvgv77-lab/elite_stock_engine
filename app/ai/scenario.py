from typing import Dict

class ScenarioNarrative:
    def generate(self, symbol: str, regime: str) -> str:
        return f"In a {regime} regime, the stock {symbol} shows notable trends in its momentum indicators."
